<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WidgetBoard.App.Views.WeatherWidgetView"
             xmlns:converters="clr-namespace:WidgetBoard.App.Converters"
             xmlns:vm="clr-namespace:WidgetBoard.App.ViewModels"
             x:DataType="vm:WeatherWidgetViewModel">

    <ContentView.Resources>
        <converters:IsEqualToStateConverter
            x:Key="isLoadingConverter"
            State="Loading" />

        <converters:IsEqualToStateConverter
            x:Key="hasLoadedConverter"
            State="Loaded" />

        <converters:IsEqualToStateConverter
            x:Key="hasErrorConverter"
            State="Error" />

        <converters:IsEqualToStateConverter
            x:Key="hasPermissionErrorConverter"
            State="PermissionError" />
    </ContentView.Resources>

    <VerticalStackLayout>
        <Label
            Text="Today"
            FontSize="20"
            VerticalOptions="Center"
            HorizontalOptions="Start"
            TextTransform="Uppercase" />

        <!-- Loading -->
        <VerticalStackLayout
            IsVisible="{Binding State, Converter={StaticResource isLoadingConverter}}">
            <ActivityIndicator
                IsRunning="{Binding State, Converter={StaticResource isLoadingConverter}}" />
            <Label
                Text="Loading weather data" />
        </VerticalStackLayout>

        <!-- Loaded -->
        <VerticalStackLayout
            IsVisible="{Binding State, Converter={StaticResource hasLoadedConverter}}">
            <Label
                VerticalOptions="Center"
                HorizontalOptions="Center">
                <Label.FormattedText>
                    <FormattedString>
                        <Span
                            Text="{Binding Temperature, StringFormat='{0:F1}'}"
                            FontSize="60" />
                        <Span
                            Text="°C" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <Label
                Text="{Binding Weather}"
                FontSize="20"
                VerticalOptions="Center"
                HorizontalOptions="Center" />
            <Image
                Source="{Binding IconUrl}"
                WidthRequest="100"
                HeightRequest="100" />
        </VerticalStackLayout>

        <!-- Error -->
        <VerticalStackLayout
            IsVisible="{Binding State, Converter={StaticResource hasErrorConverter}}">
            <Label
                Text="Unable to load weather data" />
            <Button
                Text="Retry"
                Command="{Binding LoadWeatherCommand}" />
        </VerticalStackLayout>

        <!-- PermissionError -->
        <VerticalStackLayout
            IsVisible="{Binding State, Converter={StaticResource hasPermissionErrorConverter}}">
            <Label
                Text="Unable to retrieve location data" />
            <Button
                Text="Retry"
                Command="{Binding LoadWeatherCommand}" />
        </VerticalStackLayout>
    </VerticalStackLayout>

</ContentView>